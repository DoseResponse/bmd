mtList<- function(){
fList <- list()
fList[["LL.2"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["LL.3"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["LL.4"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["LL.5"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["LL.3u"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["l2"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["l3"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["l4"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["l5"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["l3u"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["W1.2"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["W1.3"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["W1.3u"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["W1.4"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["W2.2"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["W2.3"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["W2.3u"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["W2.4"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["EXD.2"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["EXD.3"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * exp(-exp(b1 * (log(dose) - log(e1))))})
fList[["FPL.4"]] <- list(fct = function(dose,b1,c1,d1,e1,p1,p2){c1 + (d1-c1)/(1 + exp(b1 * (log(dose + 1))^p1 + e1 * (log(dose + 1))^p2))})
fList[["AR.3"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["AR.2"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1 - c1) * (1 - exp(-exp(b1 * (log(dose) - log(e1)))))})
fList[["L.5"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(dose - e1)))^f1)})
fList[["L.4"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(dose - e1)))^f1)})
fList[["L.3"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(dose - e1)))^f1)})
fList[["LL2.5"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(log(dose)-log(e1))))^f1)})
fList[["LL2.4"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(log(dose)-log(e1))))^f1)})
fList[["LL2.3"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(log(dose)-log(e1))))^f1)})
fList[["LL2.3u"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(log(dose)-log(e1))))^f1)})
fList[["LL2.2"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1-c1)/((1+exp(b1*(log(dose)-log(e1))))^f1)})
fList[["MM.3"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["MM.2"]] <- list(fct = function(dose,b1,c1,d1,e1,f1){c1 + (d1 - c1)/(1 + exp(b1 * log(dose/e1)))^f1})
fList[["LN.4"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1-c1)*(pnorm(b1*(log(dose)-log(e1))))})
fList[["LN.3"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1-c1)*(pnorm(b1*(log(dose)-log(e1))))})
fList[["LN.3u"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1-c1)*(pnorm(b1*(log(dose)-log(e1))))})
fList[["LN.2"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1-c1)*(pnorm(b1*(log(dose)-log(e1))))})
#fList[["G.4"]] <- list(fct = function(dose,b1,c1,d1,e1){c1 + (d1-c1)*(exp(-exp(b1*(dose-e1))))})
#fList[["G.3"]] <- list(fct = function(dose,b1,d1,e1){d1*(exp(-exp(b1*(dose-e1))))})
#fList[["G.2"]] <- list(fct = function(dose,b1,e1){exp(-exp(b1*(dose-e1)))})
#fList[["G.3u"]] <- list(fct = function(dose,b1,c1,e1){c1 + (1-c1)*(exp(-exp(b1*(dose-e1))))})
#fList[["gammadr"]] <- list(fct = function(dose,b1,e1){c1 + (d1-c1) * pgamma(b1*dose, e1, 1)})
fList
}
